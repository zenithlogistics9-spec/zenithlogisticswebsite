# Zenith Logistic Transport Website

French-first bilingual corporate website.

## Render deployment
Runtime: Node
Build command: `npm install`
Start command: `npm start`

The quotation endpoint is `/api/quote`. Configure SMTP variables from `.env.example` to enable email delivery.